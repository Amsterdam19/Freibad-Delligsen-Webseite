---
interface Props {
    href: string;
    label: string;
    classes: string;
}
const { href, label, classes } = Astro.props as Props;
---

<a
    role="button"
    href={href}
    class:list={["verlinkung", "btn", "btn-lg", "box-s-5", classes]}>{label}</a
>
