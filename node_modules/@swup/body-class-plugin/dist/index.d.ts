import { Handler } from 'swup';
import Plugin from '@swup/plugin';
type Options = {
    /** If set, only classes beginning with this string will be added/removed. */
    prefix: string;
};
export default class SwupBodyClassPlugin extends Plugin {
    name: string;
    requires: {
        swup: string;
    };
    defaults: Options;
    options: Options;
    constructor(options?: Partial<Options>);
    mount(): void;
    updateBodyClass: Handler<'content:replace'>;
    getBodyElement(html: string): HTMLElement;
    updateClassNames(el: HTMLElement, newEl: HTMLElement): void;
    isValidClassName(className: string): boolean | "";
}
export {};
